package code.snippet

import xml.NodeSeq

object TreeViewDemo2 {
  def render(in: NodeSeq): NodeSeq = {
    // TODO: Extract mini template for [A File] and [A Folder] and render accordingly.
    in
  }
}